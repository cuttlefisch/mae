:PROPERTIES:
:ID: term:tier
:KIND: concept
:END:
#+title: Tier
#+filetags: :terminology:

AI permission level controlling what actions the agent can take. Tiers: ReadOnly (read buffers, navigate), Write (edit buffers, create files), Shell (execute shell commands), Privileged (all operations). Set it, highest precedence first: the =MAE_AI_PERMISSIONS= env var; ~(set-option! "ai-tier" "shell")~ in =init.scm= — the primary surface; or the =[ai] auto_approve_tier= key in config.toml, which is legacy bootstrap (ADR-096). Values are case-insensitive, an unrecognised value is refused rather than guessed, and startup resolves the initial value — ~:set ai-tier <tier>~ then changes it live, taking effect on the next tool call.
