# v0.15 Two-Machine Test Notes — BOB (member / joiner)

> Fill this in as you run [`v0.15-two-machine-test-plan.md`](v0.15-two-machine-test-plan.md).
> Bob runs **no daemon** — his editor connects to alice's hub over mTLS. The decrypt/convergence
> oracles run against bob's **local** KB replica at `~/.local/share/mae/kb`.

## Environment (this machine)
- Host / OS / arch:
- Branch @ commit (`git rev-parse --short HEAD`):
- `mae --version`:
- Binary sha256 (`sha256sum target/release/mae`):
- **My editor identity fp** (`mae --collab-identity`) — send the `mae-ed25519 …` line to alice OOB: `SHA256:`
- **Alice's daemon address** (`init.scm` `collab-server-address`): `<alice-ip>:9473`
- init.scm: `collab-auth-mode "key"`, `collab-auto-connect "true"`, `collab-host-key-policy "accept-new"`  ☐
- Alice has run `mae-daemon authorize` on my key  ☐ (confirm with alice)

## Per-run canary
- `CANARY = ________` (get the value alice picked, so greps match)

## Issues (V-NN)
> Symptom → Evidence (my status/introspect + alice's daemon log) → Diagnosis → Fix → Proof

- V-01:

## Scenario log
| Scenario | My action (bob) | Expected | Actual | Status | Evidence |
|---|---|---|---|---|---|
| H1 | launch mae · `:collab-connect` · `:collab-status` · `:collab-doctor` | connected to alice; TLS ok; peer reachable | | ☐ | |
| H2 | `:collab-list` · `:collab-join <name>` · type into it | text converges byte-identical with alice | | ☐ | |
| H3 | `:kb-join collabtest` | status "pending owner approval" | | ☐ | |
| H3 | `:kb-join collabtest` (after alice approves) | "Joined (N nodes)"; see collabtest:overview/alpha/beta | | ☐ | |
| H4 | open `collabtest:alpha` (after alice edits) · then `:kb-update collabtest:beta [BOB-H4]` | I see `[ALICE-H4]`; alice sees `[BOB-H4]`; `changed=true` | | ☐ | |
| H5 | as viewer: `:kb-update collabtest:alpha [BOB-VIEWER]` | REJECTED (viewer read-only) | | ☐ | |
| H5 | as editor (restored): `:kb-update collabtest:alpha` | accepted | | ☐ | |
| H5 | try `:kb-add-member collabtest <fp> editor` | owner-only DENIED | | ☐ | |
| H6 | open `collabtest:alpha` after alice enables E2E + edits | see `SEALED-$CANARY`; `grep -r SEALED-$CANARY ~/.local/share/mae/kb` PRESENT | | ☐ | |
| H7 | stay connected after alice removes me + edits under new key | `grep -r POSTROT-$CANARY ~/.local/share/mae` EMPTY (fwd secrecy); SEALED-$CANARY still PRESENT (history) | | ☐ | |
| H8 | after alice rotates identity + edits | `collabtest:alpha` converges to `ROTID-$CANARY` | | ☐ | |
| H9 | `:collab-register-recovery-key` · `ls ~/.local/share/mae/collab/recovery/` · move offline | recovery key saved (0600) | | ☐ | |
| H9 | (fresh primary) `:collab-recover-identity <dir> <my-old-fp>` · `:kb-join collabtest` | seat inherited; `grep -r SEALED-$CANARY ~/.local/share/mae/kb` PRESENT (decrypts) | | ☐ | |
| H10 | `:collab-disconnect` · `:kb-update collabtest:beta [BOB-OFFLINE]` · `:collab-connect` | alice's `overview` shows her offline edit; my `beta` reaches alice; no loss | | ☐ | |
| H11 | offline `:kb-update collabtest:alpha [B-H11]`; reconnect | `alpha` byte-identical with alice (one value) | | ☐ | |
| H12 | `:collab-disconnect` · `:kb-update collabtest:alpha [BOB-DURABLE]` · check `durable_pending_kb_updates≥1` · quit · relaunch | pending survives restart; drains on reconnect → reaches alice | | ☐ | |
| H13 | after alice deletes daemon key + restarts: `:collab-connect` | reconnect ABORTS (host-key-changed) until I re-pin | | ☐ | |

## Handoff notes (to alice)
> e.g. "H3 pending sent — my fp is SHA256:… — approve me and I'll re-join."
