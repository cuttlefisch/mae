# v0.15 Two-Machine Test Notes — ALICE (owner + hub daemon host)

> Fill this in as you run [`v0.15-two-machine-test-plan.md`](v0.15-two-machine-test-plan.md).
> Alice owns the KB and hosts the mTLS hub daemon. Keep the daemon log teed to
> `~/mae-daemon-live.log`; drop a `===== Hn $(date -u +%FT%TZ) =====` marker before each scenario.

## Environment (this machine)
- Host / OS / arch:
- LAN IP (bob connects here):
- Branch @ commit (`git rev-parse --short HEAD`):
- `mae --version` / `mae-daemon --version`:
- Binary sha256 (`sha256sum target/release/mae daemon/target/release/mae-daemon`):
- **My editor identity fp** (`mae --collab-identity`): `SHA256:`
- **Daemon identity fp** (`mae-daemon identity`): `SHA256:`
- **Bob's editor fp** (from OOB, needed for approve/add/remove): `SHA256:`
- daemon.toml: `bind = "0.0.0.0:9473"`, `[collab.auth] mode = "key"`, `tls = true`  ☐ confirmed
- `mae-daemon authorized` lists alice + bob  ☐

## Per-run canary
- `CANARY = CANARY-________` (pick once; reuse across H6–H8)

## Issues (V-NN)
> Symptom → Evidence (log/grep) → Diagnosis (file:line if known) → Fix → Proof (commit/test)

- V-01:

## Scenario log
| Scenario | My action (alice) | Expected | Actual | Status | Evidence |
|---|---|---|---|---|---|
| H1 | launch mae · `:collab-connect` · `:collab-status` | connected; daemon logs `mTLS client authenticated` (alice) | | ☐ | |
| H2 | `:collab-share` a scratch buffer | bob can `:collab-join`; text converges | | ☐ | |
| H3 | `:kb-register collabtest tests/fixtures/kb/collabtest` · `:kb-share collabtest` | daemon `kb/share: complete` + owner=my fp | | ☐ | |
| H3 | `:kb-pending collabtest` · `:kb-approve collabtest <bob-fp> editor` | `kb/approve_member: complete`; bob joins (N nodes) | | ☐ | |
| H4 | `:kb-update collabtest:alpha [ALICE-H4]` | bob converges; `kb/node_update: applied`; `changed=true` | | ☐ | |
| H5 | `:kb-set-policy collabtest invite` · `:kb-add-member collabtest <bob-fp> viewer` | bob's next edit DENIED (`reason=role 'viewer'`) | | ☐ | |
| H5 | `:kb-add-member collabtest <bob-fp> editor` (restore) | bob edits accepted again | | ☐ | |
| H6 | `:kb-update collabtest:overview PRECANARY-$CANARY` then `:kb-set-encryption collabtest e2e` then `:kb-update collabtest:alpha SEALED-$CANARY` | `grep -r SEALED-$CANARY ~/.local/share/mae` (data_dir) EMPTY; kb/ PRESENT; PRECANARY purged | | ☐ | |
| H7 | `:kb-remove-member collabtest <bob-fp>` · `:kb-update collabtest:alpha POSTROT-$CANARY` | POSTROT absent from daemon store; I (owner) can read it | | ☐ | |
| H8 | `:collab-rotate-identity` · authorize new key · `:collab-disconnect`/`:collab-connect` | new-key `mTLS client authenticated` | | ☐ | |
| H8★ | `:kb-update collabtest:alpha ROTID-$CANARY` **and** an owner-only op (e.g. `:kb-add-member … viewer`) | **BOTH accepted** — no owner self-lockout (#266) | | ☐ | |
| H10 | edit `collabtest:overview [ALICE-WHILE-OFFLINE]` while bob offline | bob converges on reconnect; no loss | | ☐ | |
| H11 | offline: `:kb-update collabtest:alpha [A-H11]`; reconnect | `alpha` byte-identical on both; one value | | ☐ | |
| H12 | daemon `kill -9` (bob edit queued) then restart | WAL recovery; membership preserved; bob's edit converges | | ☐ | |
| H13 | delete daemon id, restart (host-key change); `tcpdump -A port 9473` | bob's reconnect aborts until re-pin; wire shows TLS not plaintext | | ☐ | |
| H13 | 3rd unauthorized editor `:collab-connect` | rejected at daemon (auth failure) | | ☐ | |

## Handoff notes (to bob)
> e.g. "at H5 now — I've demoted you to viewer, try an edit and confirm it's denied, then tell me."
