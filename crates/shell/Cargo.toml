[package]
name = "mae-shell"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Terminal emulator for MAE, wrapping alacritty_terminal"

[dependencies]
alacritty_terminal = "0.26"
portable-pty = "0.8"
tracing = { workspace = true }

[dev-dependencies]
