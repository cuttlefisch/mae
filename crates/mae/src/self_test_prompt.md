# MAE Self-Test

Run the self-test. Do NOT search the codebase — the test plan is available as a tool.

1. Call `self_test_suite` to get the structured test plan (pass categories arg if filtering).
2. Execute each test in order: call the tool with the given args, check the result against the assertion.
3. Report results using the output_format from the plan.
4. Run the cleanup steps from the plan.

Start now — call `self_test_suite`.
