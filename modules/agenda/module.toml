[module]
name = "agenda"
version = "0.1.0"
description = "Org agenda buffer keybindings and SPC o a launcher"
mae_version = ">=0.9.0"
category = "app"
depends = ["org"]

[entry]
autoloads = "autoloads.scm"
