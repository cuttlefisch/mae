;;; dashboard/init.scm — Dashboard module initialization
;;;
;;; Loaded lazily on first use of the dashboard command.
;;; Currently a no-op since the dashboard is rendered by the kernel.

(provide-feature "dashboard")
