;;; marks-jumps/init.scm — Marks and jump list initialization
;;;
;;; Loaded lazily on first use. Currently a no-op since marks/jumps
;;; logic is in the kernel (Rust dispatch).

(provide-feature "marks-jumps")
