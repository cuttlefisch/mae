[module]
name = "notifications"
version = "0.1.0"
description = "Attention bus: *Notifications* buffer keybindings + SPC n leader (ADR-024)"
mae_version = ">=0.13.0"
category = "tools"

[entry]
autoloads = "autoloads.scm"
