[module]
name = "org"
version = "0.1.0"
description = "Org-mode keybindings — headings, TODOs, babel, export, narrowing"
mae_version = ">=0.9.0"
category = "lang"

[flags]
babel = { doc = "Babel code block execution (SPC m x, C-c C-c)" }
export = { doc = "Org export to HTML/Markdown (SPC m e)" }

[entry]
autoloads = "autoloads.scm"
