[module]
name = "keymap-nonmodal"
version = "0.1.0"
description = "Non-modal (CUA-style) keybind flavor — Insert by default, C-; opens the which-key keypad"
mae_version = ">=0.9.0"
category = "keymap"

[dependencies]
keymap-leader = "*"

[entry]
autoloads = "autoloads.scm"
